# golangci-lint v2 configuration.
#
# Models are hand-written Bun structs and SQL migrations are hand-authored
# (goose) — there is no generated code directory to exclude from linting.
version: "2"

run:
  timeout: 10m
