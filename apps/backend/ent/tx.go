// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"sync"

	"entgo.io/ent/dialect"
)

// Tx is a transactional client that is created by calling Client.Tx().
type Tx struct {
	config
	// ActionItem is the client for interacting with the ActionItem builders.
	ActionItem *ActionItemClient
	// AgentAsk is the client for interacting with the AgentAsk builders.
	AgentAsk *AgentAskClient
	// AgentDMMessage is the client for interacting with the AgentDMMessage builders.
	AgentDMMessage *AgentDMMessageClient
	// AgentMemory is the client for interacting with the AgentMemory builders.
	AgentMemory *AgentMemoryClient
	// AgentToken is the client for interacting with the AgentToken builders.
	AgentToken *AgentTokenClient
	// Alert is the client for interacting with the Alert builders.
	Alert *AlertClient
	// AlertEvent is the client for interacting with the AlertEvent builders.
	AlertEvent *AlertEventClient
	// AlertInvestigation is the client for interacting with the AlertInvestigation builders.
	AlertInvestigation *AlertInvestigationClient
	// AlertInvestigationAlert is the client for interacting with the AlertInvestigationAlert builders.
	AlertInvestigationAlert *AlertInvestigationAlertClient
	// AlertInvestigationEvent is the client for interacting with the AlertInvestigationEvent builders.
	AlertInvestigationEvent *AlertInvestigationEventClient
	// AlertInvestigationUpdateEntry is the client for interacting with the AlertInvestigationUpdateEntry builders.
	AlertInvestigationUpdateEntry *AlertInvestigationUpdateEntryClient
	// AuditLog is the client for interacting with the AuditLog builders.
	AuditLog *AuditLogClient
	// CoordinationTask is the client for interacting with the CoordinationTask builders.
	CoordinationTask *CoordinationTaskClient
	// Counter is the client for interacting with the Counter builders.
	Counter *CounterClient
	// CredentialProvider is the client for interacting with the CredentialProvider builders.
	CredentialProvider *CredentialProviderClient
	// DeliveryTarget is the client for interacting with the DeliveryTarget builders.
	DeliveryTarget *DeliveryTargetClient
	// EscalationPolicy is the client for interacting with the EscalationPolicy builders.
	EscalationPolicy *EscalationPolicyClient
	// HandoffRecord is the client for interacting with the HandoffRecord builders.
	HandoffRecord *HandoffRecordClient
	// Heartbeat is the client for interacting with the Heartbeat builders.
	Heartbeat *HeartbeatClient
	// ICSRoleAssignment is the client for interacting with the ICSRoleAssignment builders.
	ICSRoleAssignment *ICSRoleAssignmentClient
	// Incident is the client for interacting with the Incident builders.
	Incident *IncidentClient
	// IncidentCoordinationMessage is the client for interacting with the IncidentCoordinationMessage builders.
	IncidentCoordinationMessage *IncidentCoordinationMessageClient
	// IncidentDocument is the client for interacting with the IncidentDocument builders.
	IncidentDocument *IncidentDocumentClient
	// IncidentInvestigation is the client for interacting with the IncidentInvestigation builders.
	IncidentInvestigation *IncidentInvestigationClient
	// IncidentInvestigationUpdateEntry is the client for interacting with the IncidentInvestigationUpdateEntry builders.
	IncidentInvestigationUpdateEntry *IncidentInvestigationUpdateEntryClient
	// IncidentTimelineEntry is the client for interacting with the IncidentTimelineEntry builders.
	IncidentTimelineEntry *IncidentTimelineEntryClient
	// Integration is the client for interacting with the Integration builders.
	Integration *IntegrationClient
	// InvestigationThread is the client for interacting with the InvestigationThread builders.
	InvestigationThread *InvestigationThreadClient
	// InvestigationThreadMessage is the client for interacting with the InvestigationThreadMessage builders.
	InvestigationThreadMessage *InvestigationThreadMessageClient
	// KnowledgeNote is the client for interacting with the KnowledgeNote builders.
	KnowledgeNote *KnowledgeNoteClient
	// MaintenanceWindow is the client for interacting with the MaintenanceWindow builders.
	MaintenanceWindow *MaintenanceWindowClient
	// Notification is the client for interacting with the Notification builders.
	Notification *NotificationClient
	// NotificationDeliveryLog is the client for interacting with the NotificationDeliveryLog builders.
	NotificationDeliveryLog *NotificationDeliveryLogClient
	// OIDCIdentity is the client for interacting with the OIDCIdentity builders.
	OIDCIdentity *OIDCIdentityClient
	// OIDCProvider is the client for interacting with the OIDCProvider builders.
	OIDCProvider *OIDCProviderClient
	// OnCallSchedule is the client for interacting with the OnCallSchedule builders.
	OnCallSchedule *OnCallScheduleClient
	// Outbox is the client for interacting with the Outbox builders.
	Outbox *OutboxClient
	// PasswordResetToken is the client for interacting with the PasswordResetToken builders.
	PasswordResetToken *PasswordResetTokenClient
	// PersonalAccessToken is the client for interacting with the PersonalAccessToken builders.
	PersonalAccessToken *PersonalAccessTokenClient
	// Playbook is the client for interacting with the Playbook builders.
	Playbook *PlaybookClient
	// PlaybookStep is the client for interacting with the PlaybookStep builders.
	PlaybookStep *PlaybookStepClient
	// PostMortem is the client for interacting with the PostMortem builders.
	PostMortem *PostMortemClient
	// RouteRules is the client for interacting with the RouteRules builders.
	RouteRules *RouteRulesClient
	// ScheduleLayer is the client for interacting with the ScheduleLayer builders.
	ScheduleLayer *ScheduleLayerClient
	// ScheduleOverride is the client for interacting with the ScheduleOverride builders.
	ScheduleOverride *ScheduleOverrideClient
	// Service is the client for interacting with the Service builders.
	Service *ServiceClient
	// ServiceDependency is the client for interacting with the ServiceDependency builders.
	ServiceDependency *ServiceDependencyClient
	// Session is the client for interacting with the Session builders.
	Session *SessionClient
	// SharedSecret is the client for interacting with the SharedSecret builders.
	SharedSecret *SharedSecretClient
	// StatusPage is the client for interacting with the StatusPage builders.
	StatusPage *StatusPageClient
	// StatusPageComponent is the client for interacting with the StatusPageComponent builders.
	StatusPageComponent *StatusPageComponentClient
	// SystemConfig is the client for interacting with the SystemConfig builders.
	SystemConfig *SystemConfigClient
	// Team is the client for interacting with the Team builders.
	Team *TeamClient
	// TeamMember is the client for interacting with the TeamMember builders.
	TeamMember *TeamMemberClient
	// TriageResult is the client for interacting with the TriageResult builders.
	TriageResult *TriageResultClient
	// TriageRule is the client for interacting with the TriageRule builders.
	TriageRule *TriageRuleClient
	// User is the client for interacting with the User builders.
	User *UserClient
	// WebhookToken is the client for interacting with the WebhookToken builders.
	WebhookToken *WebhookTokenClient

	// lazily loaded.
	client     *Client
	clientOnce sync.Once
	// ctx lives for the life of the transaction. It is
	// the same context used by the underlying connection.
	ctx context.Context
}

type (
	// Committer is the interface that wraps the Commit method.
	Committer interface {
		Commit(context.Context, *Tx) error
	}

	// The CommitFunc type is an adapter to allow the use of ordinary
	// function as a Committer. If f is a function with the appropriate
	// signature, CommitFunc(f) is a Committer that calls f.
	CommitFunc func(context.Context, *Tx) error

	// CommitHook defines the "commit middleware". A function that gets a Committer
	// and returns a Committer. For example:
	//
	//	hook := func(next ent.Committer) ent.Committer {
	//		return ent.CommitFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Commit(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	CommitHook func(Committer) Committer
)

// Commit calls f(ctx, m).
func (f CommitFunc) Commit(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Commit commits the transaction.
func (tx *Tx) Commit() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Committer = CommitFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Commit()
	})
	txDriver.mu.Lock()
	hooks := append([]CommitHook(nil), txDriver.onCommit...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Commit(tx.ctx, tx)
}

// OnCommit adds a hook to call on commit.
func (tx *Tx) OnCommit(f CommitHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onCommit = append(txDriver.onCommit, f)
	txDriver.mu.Unlock()
}

type (
	// Rollbacker is the interface that wraps the Rollback method.
	Rollbacker interface {
		Rollback(context.Context, *Tx) error
	}

	// The RollbackFunc type is an adapter to allow the use of ordinary
	// function as a Rollbacker. If f is a function with the appropriate
	// signature, RollbackFunc(f) is a Rollbacker that calls f.
	RollbackFunc func(context.Context, *Tx) error

	// RollbackHook defines the "rollback middleware". A function that gets a Rollbacker
	// and returns a Rollbacker. For example:
	//
	//	hook := func(next ent.Rollbacker) ent.Rollbacker {
	//		return ent.RollbackFunc(func(ctx context.Context, tx *ent.Tx) error {
	//			// Do some stuff before.
	//			if err := next.Rollback(ctx, tx); err != nil {
	//				return err
	//			}
	//			// Do some stuff after.
	//			return nil
	//		})
	//	}
	//
	RollbackHook func(Rollbacker) Rollbacker
)

// Rollback calls f(ctx, m).
func (f RollbackFunc) Rollback(ctx context.Context, tx *Tx) error {
	return f(ctx, tx)
}

// Rollback rollbacks the transaction.
func (tx *Tx) Rollback() error {
	txDriver := tx.config.driver.(*txDriver)
	var fn Rollbacker = RollbackFunc(func(context.Context, *Tx) error {
		return txDriver.tx.Rollback()
	})
	txDriver.mu.Lock()
	hooks := append([]RollbackHook(nil), txDriver.onRollback...)
	txDriver.mu.Unlock()
	for i := len(hooks) - 1; i >= 0; i-- {
		fn = hooks[i](fn)
	}
	return fn.Rollback(tx.ctx, tx)
}

// OnRollback adds a hook to call on rollback.
func (tx *Tx) OnRollback(f RollbackHook) {
	txDriver := tx.config.driver.(*txDriver)
	txDriver.mu.Lock()
	txDriver.onRollback = append(txDriver.onRollback, f)
	txDriver.mu.Unlock()
}

// Client returns a Client that binds to current transaction.
func (tx *Tx) Client() *Client {
	tx.clientOnce.Do(func() {
		tx.client = &Client{config: tx.config}
		tx.client.init()
	})
	return tx.client
}

func (tx *Tx) init() {
	tx.ActionItem = NewActionItemClient(tx.config)
	tx.AgentAsk = NewAgentAskClient(tx.config)
	tx.AgentDMMessage = NewAgentDMMessageClient(tx.config)
	tx.AgentMemory = NewAgentMemoryClient(tx.config)
	tx.AgentToken = NewAgentTokenClient(tx.config)
	tx.Alert = NewAlertClient(tx.config)
	tx.AlertEvent = NewAlertEventClient(tx.config)
	tx.AlertInvestigation = NewAlertInvestigationClient(tx.config)
	tx.AlertInvestigationAlert = NewAlertInvestigationAlertClient(tx.config)
	tx.AlertInvestigationEvent = NewAlertInvestigationEventClient(tx.config)
	tx.AlertInvestigationUpdateEntry = NewAlertInvestigationUpdateEntryClient(tx.config)
	tx.AuditLog = NewAuditLogClient(tx.config)
	tx.CoordinationTask = NewCoordinationTaskClient(tx.config)
	tx.Counter = NewCounterClient(tx.config)
	tx.CredentialProvider = NewCredentialProviderClient(tx.config)
	tx.DeliveryTarget = NewDeliveryTargetClient(tx.config)
	tx.EscalationPolicy = NewEscalationPolicyClient(tx.config)
	tx.HandoffRecord = NewHandoffRecordClient(tx.config)
	tx.Heartbeat = NewHeartbeatClient(tx.config)
	tx.ICSRoleAssignment = NewICSRoleAssignmentClient(tx.config)
	tx.Incident = NewIncidentClient(tx.config)
	tx.IncidentCoordinationMessage = NewIncidentCoordinationMessageClient(tx.config)
	tx.IncidentDocument = NewIncidentDocumentClient(tx.config)
	tx.IncidentInvestigation = NewIncidentInvestigationClient(tx.config)
	tx.IncidentInvestigationUpdateEntry = NewIncidentInvestigationUpdateEntryClient(tx.config)
	tx.IncidentTimelineEntry = NewIncidentTimelineEntryClient(tx.config)
	tx.Integration = NewIntegrationClient(tx.config)
	tx.InvestigationThread = NewInvestigationThreadClient(tx.config)
	tx.InvestigationThreadMessage = NewInvestigationThreadMessageClient(tx.config)
	tx.KnowledgeNote = NewKnowledgeNoteClient(tx.config)
	tx.MaintenanceWindow = NewMaintenanceWindowClient(tx.config)
	tx.Notification = NewNotificationClient(tx.config)
	tx.NotificationDeliveryLog = NewNotificationDeliveryLogClient(tx.config)
	tx.OIDCIdentity = NewOIDCIdentityClient(tx.config)
	tx.OIDCProvider = NewOIDCProviderClient(tx.config)
	tx.OnCallSchedule = NewOnCallScheduleClient(tx.config)
	tx.Outbox = NewOutboxClient(tx.config)
	tx.PasswordResetToken = NewPasswordResetTokenClient(tx.config)
	tx.PersonalAccessToken = NewPersonalAccessTokenClient(tx.config)
	tx.Playbook = NewPlaybookClient(tx.config)
	tx.PlaybookStep = NewPlaybookStepClient(tx.config)
	tx.PostMortem = NewPostMortemClient(tx.config)
	tx.RouteRules = NewRouteRulesClient(tx.config)
	tx.ScheduleLayer = NewScheduleLayerClient(tx.config)
	tx.ScheduleOverride = NewScheduleOverrideClient(tx.config)
	tx.Service = NewServiceClient(tx.config)
	tx.ServiceDependency = NewServiceDependencyClient(tx.config)
	tx.Session = NewSessionClient(tx.config)
	tx.SharedSecret = NewSharedSecretClient(tx.config)
	tx.StatusPage = NewStatusPageClient(tx.config)
	tx.StatusPageComponent = NewStatusPageComponentClient(tx.config)
	tx.SystemConfig = NewSystemConfigClient(tx.config)
	tx.Team = NewTeamClient(tx.config)
	tx.TeamMember = NewTeamMemberClient(tx.config)
	tx.TriageResult = NewTriageResultClient(tx.config)
	tx.TriageRule = NewTriageRuleClient(tx.config)
	tx.User = NewUserClient(tx.config)
	tx.WebhookToken = NewWebhookTokenClient(tx.config)
}

// txDriver wraps the given dialect.Tx with a nop dialect.Driver implementation.
// The idea is to support transactions without adding any extra code to the builders.
// When a builder calls to driver.Tx(), it gets the same dialect.Tx instance.
// Commit and Rollback are nop for the internal builders and the user must call one
// of them in order to commit or rollback the transaction.
//
// If a closed transaction is embedded in one of the generated entities, and the entity
// applies a query, for example: ActionItem.QueryXXX(), the query will be executed
// through the driver which created this transaction.
//
// Note that txDriver is not goroutine safe.
type txDriver struct {
	// the driver we started the transaction from.
	drv dialect.Driver
	// tx is the underlying transaction.
	tx dialect.Tx
	// completion hooks.
	mu         sync.Mutex
	onCommit   []CommitHook
	onRollback []RollbackHook
}

// newTx creates a new transactional driver.
func newTx(ctx context.Context, drv dialect.Driver) (*txDriver, error) {
	tx, err := drv.Tx(ctx)
	if err != nil {
		return nil, err
	}
	return &txDriver{tx: tx, drv: drv}, nil
}

// Tx returns the transaction wrapper (txDriver) to avoid Commit or Rollback calls
// from the internal builders. Should be called only by the internal builders.
func (tx *txDriver) Tx(context.Context) (dialect.Tx, error) { return tx, nil }

// Dialect returns the dialect of the driver we started the transaction from.
func (tx *txDriver) Dialect() string { return tx.drv.Dialect() }

// Close is a nop close.
func (*txDriver) Close() error { return nil }

// Commit is a nop commit for the internal builders.
// User must call `Tx.Commit` in order to commit the transaction.
func (*txDriver) Commit() error { return nil }

// Rollback is a nop rollback for the internal builders.
// User must call `Tx.Rollback` in order to rollback the transaction.
func (*txDriver) Rollback() error { return nil }

// Exec calls tx.Exec.
func (tx *txDriver) Exec(ctx context.Context, query string, args, v any) error {
	return tx.tx.Exec(ctx, query, args, v)
}

// Query calls tx.Query.
func (tx *txDriver) Query(ctx context.Context, query string, args, v any) error {
	return tx.tx.Query(ctx, query, args, v)
}

var _ dialect.Driver = (*txDriver)(nil)
