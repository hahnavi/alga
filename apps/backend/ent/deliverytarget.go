// Code generated by ent, DO NOT EDIT.

package ent

import (
	"alga/ent/alert"
	"alga/ent/deliverytarget"
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// DeliveryTarget is the model entity for the DeliveryTarget schema.
type DeliveryTarget struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Provider holds the value of the "provider" field.
	Provider deliverytarget.Provider `json:"provider,omitempty"`
	// Channel holds the value of the "channel" field.
	Channel string `json:"channel,omitempty"`
	// ChannelName holds the value of the "channel_name" field.
	ChannelName string `json:"channel_name,omitempty"`
	// PostID holds the value of the "post_id" field.
	PostID string `json:"post_id,omitempty"`
	// AlertID holds the value of the "alert_id" field.
	AlertID uuid.UUID `json:"alert_id,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DeliveryTargetQuery when eager-loading is set.
	Edges        DeliveryTargetEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DeliveryTargetEdges holds the relations/edges for other nodes in the graph.
type DeliveryTargetEdges struct {
	// Alert holds the value of the alert edge.
	Alert *Alert `json:"alert,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AlertOrErr returns the Alert value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DeliveryTargetEdges) AlertOrErr() (*Alert, error) {
	if e.Alert != nil {
		return e.Alert, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: alert.Label}
	}
	return nil, &NotLoadedError{edge: "alert"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DeliveryTarget) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case deliverytarget.FieldProvider, deliverytarget.FieldChannel, deliverytarget.FieldChannelName, deliverytarget.FieldPostID:
			values[i] = new(sql.NullString)
		case deliverytarget.FieldID, deliverytarget.FieldAlertID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DeliveryTarget fields.
func (_m *DeliveryTarget) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case deliverytarget.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case deliverytarget.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				_m.Provider = deliverytarget.Provider(value.String)
			}
		case deliverytarget.FieldChannel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field channel", values[i])
			} else if value.Valid {
				_m.Channel = value.String
			}
		case deliverytarget.FieldChannelName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field channel_name", values[i])
			} else if value.Valid {
				_m.ChannelName = value.String
			}
		case deliverytarget.FieldPostID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field post_id", values[i])
			} else if value.Valid {
				_m.PostID = value.String
			}
		case deliverytarget.FieldAlertID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field alert_id", values[i])
			} else if value != nil {
				_m.AlertID = *value
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DeliveryTarget.
// This includes values selected through modifiers, order, etc.
func (_m *DeliveryTarget) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryAlert queries the "alert" edge of the DeliveryTarget entity.
func (_m *DeliveryTarget) QueryAlert() *AlertQuery {
	return NewDeliveryTargetClient(_m.config).QueryAlert(_m)
}

// Update returns a builder for updating this DeliveryTarget.
// Note that you need to call DeliveryTarget.Unwrap() before calling this method if this DeliveryTarget
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DeliveryTarget) Update() *DeliveryTargetUpdateOne {
	return NewDeliveryTargetClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DeliveryTarget entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DeliveryTarget) Unwrap() *DeliveryTarget {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DeliveryTarget is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DeliveryTarget) String() string {
	var builder strings.Builder
	builder.WriteString("DeliveryTarget(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("provider=")
	builder.WriteString(fmt.Sprintf("%v", _m.Provider))
	builder.WriteString(", ")
	builder.WriteString("channel=")
	builder.WriteString(_m.Channel)
	builder.WriteString(", ")
	builder.WriteString("channel_name=")
	builder.WriteString(_m.ChannelName)
	builder.WriteString(", ")
	builder.WriteString("post_id=")
	builder.WriteString(_m.PostID)
	builder.WriteString(", ")
	builder.WriteString("alert_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AlertID))
	builder.WriteByte(')')
	return builder.String()
}

// DeliveryTargets is a parsable slice of DeliveryTarget.
type DeliveryTargets []*DeliveryTarget
