interface:
  display_name: "Alga Backend Patterns"
  short_description: "Follow Alga Go backend, API, store, Ent, worker, and app wiring conventions"
  default_prompt: "Use Alga Backend Patterns when writing or modifying Go backend code, stores, Ent schemas, API handlers, app wiring, workers, or backend tests."
