// Code generated by ent, DO NOT EDIT.

package ent

import (
	"alga/ent/oncallschedule"
	"alga/ent/scheduleoverride"
	"alga/ent/user"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// ScheduleOverride is the model entity for the ScheduleOverride schema.
type ScheduleOverride struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ScheduleID holds the value of the "schedule_id" field.
	ScheduleID uuid.UUID `json:"schedule_id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// StartAt holds the value of the "start_at" field.
	StartAt time.Time `json:"start_at,omitempty"`
	// EndAt holds the value of the "end_at" field.
	EndAt time.Time `json:"end_at,omitempty"`
	// CreatedBy holds the value of the "created_by" field.
	CreatedBy *uuid.UUID `json:"created_by,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ScheduleOverrideQuery when eager-loading is set.
	Edges        ScheduleOverrideEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ScheduleOverrideEdges holds the relations/edges for other nodes in the graph.
type ScheduleOverrideEdges struct {
	// Schedule holds the value of the schedule edge.
	Schedule *OnCallSchedule `json:"schedule,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ScheduleOrErr returns the Schedule value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ScheduleOverrideEdges) ScheduleOrErr() (*OnCallSchedule, error) {
	if e.Schedule != nil {
		return e.Schedule, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: oncallschedule.Label}
	}
	return nil, &NotLoadedError{edge: "schedule"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ScheduleOverrideEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ScheduleOverride) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case scheduleoverride.FieldCreatedBy:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case scheduleoverride.FieldStartAt, scheduleoverride.FieldEndAt, scheduleoverride.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case scheduleoverride.FieldID, scheduleoverride.FieldScheduleID, scheduleoverride.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ScheduleOverride fields.
func (_m *ScheduleOverride) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case scheduleoverride.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case scheduleoverride.FieldScheduleID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field schedule_id", values[i])
			} else if value != nil {
				_m.ScheduleID = *value
			}
		case scheduleoverride.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				_m.UserID = *value
			}
		case scheduleoverride.FieldStartAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field start_at", values[i])
			} else if value.Valid {
				_m.StartAt = value.Time
			}
		case scheduleoverride.FieldEndAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field end_at", values[i])
			} else if value.Valid {
				_m.EndAt = value.Time
			}
		case scheduleoverride.FieldCreatedBy:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field created_by", values[i])
			} else if value.Valid {
				_m.CreatedBy = new(uuid.UUID)
				*_m.CreatedBy = *value.S.(*uuid.UUID)
			}
		case scheduleoverride.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ScheduleOverride.
// This includes values selected through modifiers, order, etc.
func (_m *ScheduleOverride) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySchedule queries the "schedule" edge of the ScheduleOverride entity.
func (_m *ScheduleOverride) QuerySchedule() *OnCallScheduleQuery {
	return NewScheduleOverrideClient(_m.config).QuerySchedule(_m)
}

// QueryUser queries the "user" edge of the ScheduleOverride entity.
func (_m *ScheduleOverride) QueryUser() *UserQuery {
	return NewScheduleOverrideClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this ScheduleOverride.
// Note that you need to call ScheduleOverride.Unwrap() before calling this method if this ScheduleOverride
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ScheduleOverride) Update() *ScheduleOverrideUpdateOne {
	return NewScheduleOverrideClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ScheduleOverride entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ScheduleOverride) Unwrap() *ScheduleOverride {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ScheduleOverride is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ScheduleOverride) String() string {
	var builder strings.Builder
	builder.WriteString("ScheduleOverride(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("schedule_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ScheduleID))
	builder.WriteString(", ")
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("start_at=")
	builder.WriteString(_m.StartAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("end_at=")
	builder.WriteString(_m.EndAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.CreatedBy; v != nil {
		builder.WriteString("created_by=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ScheduleOverrides is a parsable slice of ScheduleOverride.
type ScheduleOverrides []*ScheduleOverride
